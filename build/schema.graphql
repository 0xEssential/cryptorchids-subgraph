enum GrowthStage {
  FLOWERING
  DEAD
}

type CryptOrchid @entity {
  id: ID!
  tokenId: Int!
  owner: Bytes!
  latinSpeciesName: String!
  plantedAt: Int!
  waterLevel: Int!
  growthStage: GrowthStage!
}
